---
title: "Motrix: Full-Featured Cross-Platform Download Manager"
sidebarTitle: "Introduction"
description: "Motrix is a clean desktop download manager for HTTP, FTP, BitTorrent, and magnet links, with plugins, browser extensions, a CLI, and Docker server mode."
---

Motrix is a clean, full-featured desktop download manager that handles HTTP, FTP, BitTorrent, magnet links, and more — all from a single intuitive interface. Built on Electron, React, and TypeScript, Motrix keeps the download core independent of the UI so the same engine powers both a polished desktop app and a headless Docker server. Browser extensions, command-line tools, and plugins all communicate over **MDXP** (Motrix Download eXchange Protocol), an open JSON-RPC 2.0 protocol that keeps the ecosystem consistent and extensible.

## Key Features

<CardGroup cols={2}>
  <Card title="HTTP & FTP Downloads" icon="arrow-down-to-line" href="/features/downloads">
    Download files over HTTP, HTTPS, and FTP with multi-connection acceleration, per-task speed limits, and SQLite-backed session recovery after a restart.
  </Card>
  <Card title="BitTorrent & Magnets" icon="magnet" href="/features/bittorrent">
    Full BitTorrent support with per-file selection, built-in tracker list management with health checks, and UPnP / NAT-PMP port mapping.
  </Card>
  <Card title="Plugin System" icon="puzzle-piece" href="/integrations/plugins">
    QuickJS-sandboxed plugins with fine-grained permissions run URL Resolvers, filename templates, and page scrapers — all installable from the in-app marketplace.
  </Card>
  <Card title="CLI Client" icon="terminal" href="/integrations/cli">
    The official `@motrix/cli` package provides the `motrix` command for everyday shell use and AI agents, with automatic local-app discovery and remote pairing.
  </Card>
  <Card title="Browser Extensions" icon="browser">
    Chrome and Firefox extensions (Manifest V3) intercept downloads in one click and hand them off to Motrix over native messaging.
  </Card>
  <Card title="Docker Server" icon="docker" href="/server/docker-deployment">
    Run Motrix headless on a NAS or home server. A multi-arch Docker image includes a web UI and secure device-code pairing for remote CLI and agent clients.
  </Card>
</CardGroup>

## Ecosystem

Motrix extends beyond the desktop app with a shared protocol library, command-line client, browser extensions, and a complete plugin toolchain.

| Project | Distribution | What it does |
|---------|--------------|--------------|
| [`@motrix/mdxp`](https://github.com/motrixapp/mdxp) | npm package | Defines the shared JSON-RPC 2.0 wire schemas and Zod types for MDXP, with helpers for bidirectional connections |
| [`@motrix/cli`](https://github.com/motrixapp/cli) | npm package | Provides the `motrix` command, automatically discovers a local desktop app, and pairs with remote instances |
| Motrix Browser Extension | Browser extension | Intercepts downloads in Chrome and Firefox (Manifest V3), hands them off to Motrix, and pairs securely with the desktop app over native messaging |
| [Motrix Plugin SDK](https://github.com/motrixapp/plugin-sdk) | Four npm packages | Includes `@motrix/plugin-manifest-schema`, `@motrix/plugin-api`, `@motrix/plugin-cli`, and `create-motrix-plugin` for developing, testing, and packaging plugins |
| [Builtin Plugins](https://github.com/motrixapp/builtin-plugins) | Signed `.moext` packages | Filename Template, Page Scraper, and URL Resolver — three official plugins shipped with every release |
| Plugin Registry | Public JSON feed | Publishes plugin listings and install metadata at `dl.motrix.app/registry/plugins.json` for the website and in-app marketplace |

## Tech Stack

Motrix is built with a modern, strictly layered codebase. CI enforces the dependency boundaries between layers, keeping the core portable and independent of the UI.

| Area | Stack |
|------|-------|
| Desktop shell | Electron 43 |
| UI | React 19 + Tailwind CSS 4 + shadcn/ui |
| Language | TypeScript in strict mode |
| Build system | Vite 8 with separate main, preload, worker, and renderer targets |
| Validation | Zod 4 for settings, IPC payloads, and wire schemas |
| Download engine | A Motrix-maintained fork of aria2, bundled with the app |
| Persistence | better-sqlite3 for download session storage and recovery |
| Plugin sandbox | quickjs-emscripten |
| Server runtime | Node.js + Fastify + WebSocket |
| Internationalization | i18next + react-i18next |

## Motrix Turbo v2

**Motrix Turbo** is Motrix v2, rebuilt from the ground up while preserving the clean, straightforward experience of v1. The download core is fully decoupled from the UI — the desktop app and headless Docker server share the same core engine, and platform-specific capabilities like notifications and secret storage have separate implementations with consistent behavior.

<Note>
  Motrix Turbo v2 is currently in beta. Back up your existing Motrix data and downloads before testing. Migration from Motrix v1 data has not yet been validated — do not use your only copy of v1 data with this beta.
</Note>
